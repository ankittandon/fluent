[package]
name = "screamer-core"
version = "1.0.0"
edition = "2021"
license = "MIT"
publish = false

[lib]
name = "screamer_core"
path = "src/lib.rs"

[build-dependencies]
cc = "1"

[dependencies]
serde = { version = "1", features = ["derive"] }
